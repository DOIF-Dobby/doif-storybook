import React from 'react';

interface SampleProps {}

/**
 * `Sample` 컴포넌트는 개발자들이 React와 해당 컴포넌트들로 개발할 때 참고할 수 있는 코드를 보여줍니다. export 되지 않습니다.
 */
function Sample(props: SampleProps) {
  return <div></div>;
}

export default Sample;
